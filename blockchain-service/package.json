{
  "name": "digitrans-cm-blockchain",
  "version": "1.0.0",
  "description": "Smart Contracts for AGROCAM Supply Chain Traceability",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:aws": "hardhat run scripts/deploy.js --network aws"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
